name: Conan Linux Build

on:
  push:
    branches: [ main, conan-integration ]
  pull_request:
    branches: [ main ]

jobs:
  # Check if this is a Conan integration (skip if not)
  check-conan:
    runs-on: ubuntu-latest
    outputs:
      skip-legacy: ${{ steps.check.outputs.skip-legacy }}
    steps:
    - uses: actions/checkout@v4
    - id: check
      run: |
        if [[ -f "conanfile.py" ]]; then
          echo "skip-legacy=false" >> $GITHUB_OUTPUT
          echo "🚀 Conan integration detected"
        else
          echo "skip-legacy=true" >> $GITHUB_OUTPUT
          echo "📦 No conanfile.py found, skipping Conan workflow"
        fi

  linux-build:
    needs: check-conan
    if: needs.check-conan.outputs.skip-legacy == 'false'
    runs-on: ubuntu-24.04
    
    strategy:
      matrix:
        profile: 
          - bb_linux_ubuntu_24_10_x86_64_release_gcc_14
        config:
          - { preset: "default", opts: "-o 'baresip/*:with_sdl=False' -o 'baresip/*:with_gstreamer=False' -o 'baresip/*:with_gtk=False' -o 'baresip/*:with_pipewire=True' -o 'baresip/*:with_sndfile=False' -o 'baresip/*:with_av1=True' -o 'baresip/*:with_ffmpeg=True' -o 'baresip/*:with_vpx=True' -o 'baresip/*:with_pulseaudio=False' -o 'baresip/*:with_mpg123=False'" }

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          build-essential \
          cmake \
          gcc-14 \
          g++-14 \
          pkg-config \
          python3 \
          python3-pip \
          python3-venv
        
        # Set GCC 14 as default
        sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-14 100
        sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-14 100

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install Conan
      run: |
        python -m pip install --upgrade pip
        pip install "conan>=2.0"

    - name: Setup Conan
      run: |
        conan profile detect --force
        
        # Add test-conan remote with higher priority
        conan remote add test-conan http://13.61.152.119:9300 --force
        conan remote list
        
        # Login to test-conan
        echo "${{ secrets.CONAN_PASSWORD }}" | conan remote login test-conan ${{ secrets.CONAN_USER }}
        
        # Install bbconanconfig
        conan config install-pkg bbconanconfig/0.9.5
        
    - name: Build with Conan
      run: |
        echo "🔨 Building baresip with profile: ${{ matrix.profile }}"
        echo "📋 Configuration: ${{ matrix.config.preset }}"
        
        conan create . \
          -pr:h ${{ matrix.profile }} \
          -pr:b ${{ matrix.profile }} \
          ${{ matrix.config.opts }} \
          --build=missing
          
    - name: Upload packages to artifactory
      if: success()
      run: |
        echo "📤 Uploading built packages to test-conan remote..."
        
        # Upload baresip package
        conan upload "baresip/4.0.0" -r=test-conan --confirm
        
        # Upload any other packages that might have been built
        conan list "*" --cache | grep -E "^[a-zA-Z]" | while read package; do
          if [[ "$package" != "baresip"* ]]; then
            echo "📦 Checking if $package was built and needs upload..."
            conan upload "$package" -r=test-conan --confirm || true
          fi
        done
        
    - name: Verify installation
      run: |
        echo "🧪 Testing deployment..."
        mkdir -p test-deploy && cd test-deploy
        
        cat > conanfile.txt << 'EOF'
        [requires]
        baresip/4.0.0
        
        [options]
        baresip/*:with_sdl=False
        baresip/*:with_pipewire=True
        baresip/*:with_av1=True
        baresip/*:with_ffmpeg=True
        baresip/*:with_vpx=True
        
        [generators]
        VirtualBuildEnv
        EOF
        
        conan install . -d full_deploy -pr:h ${{ matrix.profile }}
        
        # Test that baresip runs and shows correct version
        ./full_deploy/host/baresip/4.0.0/Release/x86_64/bin/baresip --version | head -1
        
        # Verify it shows v4.0.0
        if ./full_deploy/host/baresip/4.0.0/Release/x86_64/bin/baresip --version | grep -q "baresip v4.0.0"; then
          echo "✅ Version verification passed"
        else
          echo "❌ Version verification failed"
          exit 1
        fi
